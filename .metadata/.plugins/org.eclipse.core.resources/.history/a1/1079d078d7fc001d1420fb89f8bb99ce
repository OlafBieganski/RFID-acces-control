#include "keypad.h"

int8_t scan_pressed_index(){
	for(uint8_t l_col = 0; l_col < NUM_OF_ROWS; l_col++){
		HAL_GPIO_WritePin(column_ports_array[l_col],
						  column_pins_array[l_col],
						  GPIO_PIN_RESET);

		for(uint8_t l_row = 0; l_row < NUM_OF_ROWS; l_row++){

		}
	}
}
